import 'package:flutter/material.dart';

class nas_xx_roughClass extends StatelessWidget {
  const nas_xx_roughClass({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
